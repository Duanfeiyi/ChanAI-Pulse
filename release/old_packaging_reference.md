# Old Packaging Reference

This file is a stage-1 placeholder for documenting the old draft packaging approach.

The old GitHub draft repository `eaglexene/ChanAI-Pulse` is read-only reference material only. The v1.0 release candidate will not push to, pull from, overwrite, or delete content in that repository.

Known from the project request and preliminary read-only review:

- The old draft repository appeared to include an `application/` folder.
- It included `ApplicationInstallerManifest.xml`.
- It included `ChannelSimulatorInstaller.exe`.
- It included a `README.md` and image/icon assets.

Preliminary interpretation: this looks like MATLAB App packaging output, likely produced through MATLAB Compiler/App packaging tooling. Full reproduction steps are deferred until stage 2, when old packaging files can be inspected more completely and the local MATLAB Compiler availability issue can be resolved or documented.

