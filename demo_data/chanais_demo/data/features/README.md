# Derived feature placeholder

This directory is intentionally present because the ChanAIs demo contract expects a `data/features/` location. Step 1 does not add derived feature arrays; later steps may place small, synthetic, documented fixtures here.
