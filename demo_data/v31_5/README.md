# v3.1-5 target-free P8 requests

These two one-example JSON files are public, synthetic, target-free smoke-test requests. Their known P8 inputs equal the frozen Registry training means; they demonstrate package loading and model selection, not prediction accuracy.

- `requests/interpolation_neutral_p8_request.json`
- `requests/extrapolation_neutral_p8_request.json`

No target parameters or measured channel data are included.
