function [cx, cy, cz] = set_coordinate(x, y, z)
    %--------------------------------------------------------------------------------------------
    % SET_COORDINATE: Set the coordinate of a point by assigning the value at the same time
    %--------------------------------------------------------------------------------------------

    cx = x;
    cy = y;
    cz = z;

end

